<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Intelicense.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinLicenseInfo"
    MinWidth="720"
    MinHeight="600">

    <Grid RowDefinitions="Auto,*,Auto" Padding="16">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <Button
                Content="Get License Info"
                Command="{Binding GetLicenseInfoCommand}" />
            <CheckBox
                Content="Use PowerShell as fallback"
                IsChecked="{Binding UsePowerShellFallback, Mode=TwoWay}" />
            <CheckBox
                Content="Show sensitive keys (requires confirmation)"
                IsChecked="{Binding ShowSensitiveKeys, Mode=TwoWay}" />
            <ProgressRing
                Width="28"
                Height="28"
                Margin="8,0,0,0"
                VerticalAlignment="Center"
                IsActive="{Binding IsBusy}"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,16" VerticalScrollBarVisibility="Auto">
            <Grid ColumnDefinitions="Auto,*" RowSpacing="12" ColumnSpacing="16">
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="IsReadOnly" Value="True" />
                        <Setter Property="TextWrapping" Value="Wrap" />
                    </Style>
                </Grid.Resources>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Product Name" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LicenseInfo.ProductName}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Edition ID" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding LicenseInfo.EditionId}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Product ID" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding LicenseInfo.ProductId}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Current Build" />
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding LicenseInfo.CurrentBuild}" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Installation Type" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding LicenseInfo.InstallationType}" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Partial Product Key" />
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding LicenseInfo.PartialProductKey}" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="OA3 Original Product Key" />
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding LicenseInfo.Oa3OriginalProductKey}" />

                <TextBlock Grid.Row="7" Grid.Column="0" Text="Decoded Product Key" />
                <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding LicenseInfo.DecodedProductKey}" />

                <TextBlock Grid.Row="8" Grid.Column="0" Text="Product Type Code" />
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding ProductTypeDisplay}" />

                <TextBlock Grid.Row="9" Grid.Column="0" Text="License Status" />
                <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding LicenseInfo.LicenseStatus}" />

                <TextBlock Grid.Row="10" Grid.Column="0" Text="Retrieval Sources" />
                <TextBox
                    Grid.Row="10"
                    Grid.Column="1"
                    MinHeight="120"
                    AcceptsReturn="True"
                    Text="{Binding RetrievalSourcesDisplay}" />
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
            <Button
                Content="Export to JSON"
                Command="{Binding ExportToJsonCommand}" />
            <Button
                Content="Copy to Clipboard"
                Command="{Binding CopyToClipboardCommand}" />
            <TextBlock
                Margin="16,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding StatusMessage}" />
        </StackPanel>
    </Grid>
</Window>
